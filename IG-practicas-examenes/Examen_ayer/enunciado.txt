crea una clase derivada de mallaInd para una malla indexada de triangulos como la de la figura, la malla tiene 10 vertices y 8 triangulos. asignale colores a los vertices de forma que los 6 vertices en el plano inferior se vean de un tono gris oscuro y los 2 vertices de arriba segun la figura, se vean de color blanco




 usando exclusivamente una instancia de la malla indexada del cubo de la práctica 1, crea un modelo jerarquico con dos primas, uno sobre el otro. ambos tienen ancho w y altura h (donde h y w son parámetros del constructor) el modelo tiene dos grados de libertad. al animar el modelo, el prisma superior rota (oscilando entre dos angulos) entorno a un eje que pasa por una arista en comun entre los dos prismas, mientras que el prisma inferior se acorta y alarga en y (manteniendo el ancho). elige la amplitud y frecuencia de ambos movimientos, de forma que no sean sincronos (que tengan periodos distintos).





 crea una clase derivada de mallaInd para una malla indexada de trinagulos como la de la figura derecha, la cual consta de n réplicas de la malla indexada
de la pregunta anterior (la de los triángulos), donde (n>=1) es un parámetro del constructor (la figura se ha hecho n==7). Cada réplica está desplazada respecto de la anterior.
La figura tiene 8n triángulos. Los vértices entre dos réplicas adyacentes pueden estar repetidos, y en ese caso habrá 10n vértices.
